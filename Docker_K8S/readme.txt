# this file contains the procedure to run application using docker kubernetes.

first keep the trained model in parellel to dockerfile and code or anywhere in local.
then load the model using model_loader.py
creat app.py conytaining the flask api.
create docker file and service and deployment.yml and requirements.txt.
after deploying the service and deployment.yml get the dns and run runapplication.py code
