# name: MLOps CI Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   run-pipeline:
#     runs-on: ubuntu-latest

#     permissions:
#       id-token: write
#       contents: read

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Setup Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.10"

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Verify SageMaker Install
#         run: |
#           python -c "from sagemaker.workflow.pipeline import Pipeline; print('SageMaker OK')"

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           role-to-assume: arn:aws:iam::218736973000:role/githubrole
#           aws-region: eu-north-1
      
#       - name: Verify AWS Identity
#         run: aws sts get-caller-identity

#       - name: Test S3 Access
#         run: aws s3 ls s3://placement-project-bkt

#       - name: Run SageMaker Pipeline
#         run: |
#           python pipeline/pipeline.py
